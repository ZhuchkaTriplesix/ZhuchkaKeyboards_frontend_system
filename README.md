# Zhuchka System (Flutter web)

Internal / admin UI. This repo targets **web only** (no mobile/desktop platforms in the tree).

## Run (Chrome)

```bash
flutter run -d chrome
```

## Build static site

```bash
flutter build web
```

Output: `build/web/`.

## Tests

```bash
flutter test
```
